package main;

import entities.*;

public class Level {
	Player p = new Player(350, 400);
	Enemy[] ea = new Enemy[10];//Muss erhöht werden
	
	public void levelactualisizer() { // so richtig falsch geschrieben
		if(Player.px == 800) {
			
		}
	}
	
	public void createLvl(int lvl) {
		switch(lvl) {
		case 1:
		}
	}
}
